---
# This file was migrated from components/parameters.yaml (filter)
# Reusable query parameter for filter
filter:
  name: filter
  in: query
  required: false
  schema:
    type: string
  description: filter criteria in the format field:operator:value (e.g., name:contains:test)
  example: "name:contains:acme"

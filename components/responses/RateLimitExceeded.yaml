# This file was migrated from components/responses.yaml (RateLimitExceeded)
RateLimitExceeded:
  description: Rate limit exceeded
  content:
    application/json:
      schema:
        $ref: "../../schemas/common/error.yaml#/Error"
      example:
        code: RATE_LIMIT_EXCEEDED
        message: Rate limit exceeded

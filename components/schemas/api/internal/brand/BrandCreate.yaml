type: object
required:
  - brandInfo
properties:
  brandInfo:
    allOf:
      - $ref: "../../../common/base/BrandInfo.yaml"
      - required: [name]
description: >
  Payload for creating a new Brand. 'brandInfo.name' and 'brandInfo.userId' is required.

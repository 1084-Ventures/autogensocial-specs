---
BaseModel:
  type: object
  required:
    - id
    - metadata
  properties:
    id:
      type: string
      description: unique identifier for the record
    metadata:
      $ref: "./Metadata.yaml#/Metadata"

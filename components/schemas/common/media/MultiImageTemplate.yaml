type: object
properties:
  imageTemplates:
    type: array
    items:
      $ref: "./ImageTemplate.yaml"
    description: Array of image objects
  numImages:
    type: integer
    description: The exact number of images required

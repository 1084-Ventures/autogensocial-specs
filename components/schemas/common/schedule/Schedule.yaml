type: object
required:
  - days_of_week
  - time_slots
properties:
  daysOfWeek:
    type: array
    items:
      type: string
      enum: [monday, tuesday, wednesday, thursday, friday, saturday, sunday]
  timeSlots:
    type: array
    items:
      $ref: "./TimeSlot.yaml"

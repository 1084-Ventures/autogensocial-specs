type: object
properties:
  color:
    type: string
  transparency:
    type: number
  verticalLocation:
    type: string
    enum: [top, middle, bottom]
  horizontalLocation:
    type: string
    enum: [left, middle, right]

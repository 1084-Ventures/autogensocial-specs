allOf:
  - $ref: "../common/base/BaseModel.yaml"
  - type: object
    properties:
      brandId:
        type: string
      lastExecutionTime:
        type: string
        format: date-time
        description: Last time the template was executed
      templateInfo:
        $ref: "../common/template/TemplateInfo.yaml"
      schedule:
        $ref: "../common/schedule/Schedule.yaml"
      templateSettings:
        $ref: "../common/template/TemplateSettings.yaml"

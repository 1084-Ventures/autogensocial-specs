allOf:
  - $ref: "../common/base/BaseModel.yaml"
  - type: object
    properties:
      brandId:
        type: string
      blobUrl:
        type: string
      mediaType:
        type: string
        enum:
          - image
          - video
      usageCount:
        type: integer
      mediaMetadata:
        $ref: "../common/media/MediaMetadata.yaml"

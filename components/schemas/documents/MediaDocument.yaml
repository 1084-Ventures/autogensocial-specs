---
MediaDocument:
  allOf:
    - $ref: "../common/base/BaseModel.yaml#/BaseModel"
    - type: object
      properties:
        brandId:
          type: string
        blobUrl:
          type: string
        mediaType:
          type: string
          enum:
            - image
            - video
        mediaMetadata:
          $ref: "../common/media/MediaMetadata.yaml#/MediaMetadata"

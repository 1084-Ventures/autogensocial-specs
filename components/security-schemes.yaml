---
bearerAuth:
  type: http
  scheme: bearer
  bearerFormat: JWT
ApiKeyAuth:
  type: apiKey
  description: api key authentication for all endpoints
  name: x-api-key
  in: header
  x-example: "your-api-key-here"

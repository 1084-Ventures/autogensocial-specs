---
paths:
  /media/{id}:
    delete:
      summary: Delete a media file
      parameters:
        - $ref: "../../../components/parameters.yaml#/id"
      responses:
        "200":
          description: Deleted
        "404":
          $ref: "../../../components/responses.yaml#/NotFound"

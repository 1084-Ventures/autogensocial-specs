openapi: 3.0.0
components:
  schemas:
    BaseModel:
      type: object
      required:
        - id
        - metadata
      properties:
        id:
          type: string
          description: Unique identifier for the record
        metadata:
          $ref: './common/metadata.yaml'